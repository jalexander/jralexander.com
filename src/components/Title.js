import styled from 'styled-components'
import styles from '../config/styles'

export default styled.h1`
  margin: 0 0 20px 0;
  display: block;
  line-height: 1;

  opacity: 1;
  color: ${styles.green};
  font-family: ${styles.pathwayGothic};

  font-size: 40px;
  font-weight: 100;
`
